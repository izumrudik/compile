#!jararaca.py -i
from std.builtin import putd, putendl, putsln, putd, puts, int, nth_bit, char, eputs, exit, stdin, short, read, get_arg, parse_int #@default
fun main {
	if Argc != 3 {
		puts("Usage: ")
		puts(get_arg(0))
		puts(" <number> <number>\n")
		exit(1)
	}
	a = parse_int(get_arg(1))
	b = parse_int(get_arg(2))
	putd(a+b)
	putendl()
}